import 'package:flutter/material.dart';

class DividerPersonalizada extends StatelessWidget {
  const DividerPersonalizada({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Color.fromARGB(255, 70, 70, 70),
    );
  }
}
